#include <stdio.h>

int main()
{
    // Expressão inicial
    int i = 0;

    while (i <= 10) // Expressão booleana
    {
        printf("%d\n", i);

        //i = i + 1;
        i++; // Expressão de incremento
    }
    

    return 0;
}